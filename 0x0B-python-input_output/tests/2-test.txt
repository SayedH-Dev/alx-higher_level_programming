#!/usr/bin/python3

# Checking for module docstring:
>>> m = __import__('2-append_write').__doc__
>>> len(m) > 1
True

# Checking for append_write() function docstring:
>>> append_write_doc = __import__('2-append_write').append_write.__doc__
>>> len(append_write_doc) > 1
True

# Test 1: Appending to an Existing File
>>> result = __import__('2-append_write').append_write("file.txt", "This is an append test")\
... # Check that the number of characters added matches the expected value
>>> expected_characters_added = len("This is an append test")
>>> result == expected_characters_added
True

# Test 2: Appending to a Non-Existing File
>>> resutl = __import__('2-append_write').append_write("file_a.txt", "Creating and appending")\
... # Check that the number of characters added matches the expected value
>>> expected_characters_added = len("Creating and appending")
>>> result == expected_characters_added
True

